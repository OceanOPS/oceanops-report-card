# Scripts moved

Partner and GeoJSON export scripts live in **[oceanops-data-exports](https://github.com/OceanOPS/oceanops-data-exports)**.

From the report-card repo:

```bash
npm run export:partners
```

Or from `../oceanops-data-exports`: `npm run export:partners` / `npm run export:all`.
